#include<stdio.h>
#include<stdib.h>
struct node{
  int data;
  struct node *l
  struct node *r
};
struct node*create(int value)
{
  struct node*p;
  p=(struct node*)malloc(size of(struct node));
  p->l=Null;
  p->r=Null;
};
preorder(struct node *root);
if (root!=null){
  printf("%d", root->data);
  preorder(root->l);
  preorder(root->r);
};
int countnode(int data)
{
  if(data==null)
  return0;
  else
  return(l+1->data+r->data);
}
int main(){
  struct node *n1,*n2,*n3,*n4;
  n1=(struct node*)malloc(size of(struct node));
  n2=(struct node*)malloc(size of(struct node));
  n3=(struct node*)malloc(size of(struct node));
  n4=(struct node*)malloc(size of(struct node));
  n1->data=data;
  n1->1=n2;
  n1->1=n3;
  n3->1=n4;
  n4->r=Null;
}
